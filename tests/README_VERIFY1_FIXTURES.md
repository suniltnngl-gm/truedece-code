# VERIFY-1 Phase A Fixture Work

This directory is reserved for immutable verification fixtures. Reference values must be independently recorded before the implementation-under-test is executed against them.

Do not generate expected values from the implementation during a verification run.
