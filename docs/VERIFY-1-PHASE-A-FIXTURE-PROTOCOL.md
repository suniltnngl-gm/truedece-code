# VERIFY-1 Phase A Fixture Protocol

V1 and V4 remain PARTIAL pending independently recorded immutable fixtures and explicit deep-state equivalence evidence. Fixtures must not be generated from the implementation under test during acceptance. Failures are verification findings; no implementation changes are made merely to fit fixtures.
